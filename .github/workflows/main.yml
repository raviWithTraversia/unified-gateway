on:
  push:
    branches:
      - main

name: 🚀 Deploy website on push

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: 📂 kafila2
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 43.242.214.135
          username: kafila2
          password: '${{ secrets.prop_secret_password }}'
          port: 21         
          debug: true
